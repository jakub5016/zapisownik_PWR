# #:kivy 2.2.1

<WelcomePage>:
    canvas:
        Color:
            rgba: self.page_color

        RoundedRectangle:
            pos: self.center_x/2, self.center_y/2
            size: self.parent.width/2, self.parent.height/2
            radius: [(60, 60), (60, 60), (60, 60), (60, 60)]

    LoginButton:
        center: self.parent.center

<RootClass>:
    canvas:
        Color:
            rgba: self.background_color
        Rectangle:
            size: self.width, self.height

    WelcomePage:
        center: self.parent.center

    Label:
        font_size: self.width/3
        center_x: self.parent.width / 2
        center_y: (self.parent.height / 2) + (0.1 * self.parent.height)
        text: "Witaj w zapisowniku!"
        font_name: "Roboto-Black.ttf"
        color: self.parent.text_color


<LoginButton>:
    canvas:
        Color:
            rgba : 102/255, 155/255, 188/255, 1

        RoundedRectangle:
            radius: [(60, 60), (60, 60), (60, 60), (60, 60)]
            size: self.width, self.height
            pos: self.pos
    Label:
        text:"Zaloguj sie"
        background_color: self.background_color
        background_normal: ""
        background_down: ""
        center_x: self.parent.width / 2
        center_y: (self.parent.height / 2) - (0.1 * self.parent.height)
        font_name: "Roboto-Black.ttf"
        color: 240/255, 240/255, 240/255, 1

