# #:kivy 2.2.1

<WelcomePage>:
    canvas:
        Color:
            rgba: self.main_color_1

        RoundedRectangle:
            pos: self.center_x/2, self.center_y/2
            size: self.parent.width/2, self.parent.height/2
            radius: [(60, 60), (60, 60), (60, 60), (60, 60)]

    Label:
        font_size: self.width/3
        center_x: self.parent.width / 2
        center_y: (self.parent.height / 2) + (0.1 * self.parent.height)
        text: "Witaj w zapisowniku!"
        font_name: "Roboto-Black.ttf"
        color: self.parent.text_color

    LoginButton:
        center_x: self.parent.center_x
        center_y: self.parent.center_y - (0.1 * self.parent.height)


<LoginButton>:
    background_normal: "./transparent_background.png"
    background_down: "./transparent_background.png"
    on_press:
        self.press()
        self.parent.parent.manager.transition.duration = 0.7
        self.parent.parent.manager.current = "screen_two"


    canvas:
        Color:
            rgba : self.button_color

        RoundedRectangle:
            radius: [(60, 60), (60, 60), (60, 60), (60, 60)]
            size: self.width, self.height
            pos: self.pos
    Label:
        text:"Zaloguj sie"
        background_color: self.background_color
        center_x: self.parent.center_x
        center_y: self.parent.center_y
        font_name: "Roboto-Black.ttf"
        color: self.parent.text_color

<ScreenTwo>:
    canvas:
        Color:
            rgba: self.background_color
        Rectangle:
            size: self.width, self.height

    LoginPage:


<ScreenOne>:
    canvas:
        Color:
            rgba: self.background_color
        Rectangle:
            size: self.width, self.height

    WelcomePage:

<LoginPage>:
    canvas:
        Color:
            rgba: self.page_color

        RoundedRectangle:
            pos: self.center_x/2, self.center_y/2 - (0.15 * self.center_y/2)
            size: self.parent.width/2, self.parent.height/1.7
            radius: [(60, 60), (60, 60), (60, 60), (60, 60)]

    Label:
        font_size: self.width/3
        center_x: self.parent.width / 2
        center_y: (self.parent.height / 2) + (0.2 * self.parent.height)
        text: "Zaloguj się!"
        font_name: "Roboto-Black.ttf"
        color: self.parent.text_color
